run-tests:
	@ cargo test

open-docs:
	@ cargo doc --open

check-format:
	@ cargo fmt --all -- --check

code-format:
	@ cargo fmt --all

publish-dry-run:
	@ cargo publish --dry-run

publish:
	@ cargo publish

generate-report:
	@ ./bin/coverage-report.sh --open